\section{Training}

\begin{frame}
    \frametitle{Training Objective and Early Stopping}
    \footnotesize
    \begin{columns}[t]
        \begin{column}{0.50\textwidth}
            \begin{block}{Loss}
                \[
                    \begin{aligned}
                        L ={}& L_{BCE}(y_{event}, \hat{y}_{event}) \\
                              & + 0.15 L_{MSE}(y_{count}, \hat{y}_{count})
                    \end{aligned}
                \]
                Positive event samples use weight 3.0 to counter sparse labels.
            \end{block}
        \end{column}
        \begin{column}{0.45\textwidth}
            \begin{table}
                \centering
                \begin{tabular}{lr}
                    \toprule
                    Setting & Value \\
                    \midrule
                    Optimizer & AdamW \\
                    Learning rate & $10^{-3}$ \\
                    Batch size & 256 \\
                    Patience & 6 epochs \\
                    Best epoch & 4 \\
                    \bottomrule
                \end{tabular}
            \end{table}
        \end{column}
    \end{columns}

    \begin{exampleblock}{Training history signal}
        Validation loss is almost flat from epoch 1 to epoch 4 (0.3736 $\rightarrow$ 0.3732), then degrades to 0.4052 by epoch 10 while train loss keeps falling. Early stopping prevents the count head from overfitting the training windows.
    \end{exampleblock}
\end{frame}